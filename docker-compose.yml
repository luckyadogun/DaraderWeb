version: "3.1"
services:
  db:
    image: postgres:9.6.9-alpine
    environment:
      POSTGRES_USER: tmb
      POSTGRES_PASSWORD: tmb
      POSTGRES_DB: tmb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
